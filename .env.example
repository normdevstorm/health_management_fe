# Environment Variables for Health Management App

# Storage Encryption Key (REQUIRED for production)
# Generate a secure key using: openssl rand -base64 32
STORAGE_ENCRYPTION_KEY=your_32_character_secure_key_here

# API Base URL
API_BASE_URL=https://api.example.com

# Instructions:
# 1. Copy this file to .env (DO NOT commit .env to git)
# 2. Generate a secure encryption key:
#    openssl rand -base64 32
# 3. Replace the values with your actual keys
# 4. Use these values in launch.json or build commands
